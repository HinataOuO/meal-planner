interface:
  display_name: "Meal Planner"
  short_description: "Meal plans, macros, prep, and check-ins"
  default_prompt: "Use $meal-planner: collect missing profile/safety first, then create calorie/macro targets, meal prep, shopping list."
